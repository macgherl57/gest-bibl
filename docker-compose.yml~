version: '3.7' # specify docker-compose version
# Define the services/containers to be run
services:
  angular: # name of the first service
    hostname: localhost
    build:
      context: .
      dockerfile: Dockerfile-angular
    ports:
      - "8181:80" # specify port forwarding
  express: #name of the second service
    build: restfulapi_orm # specify the directory of the Dockerfile
    ports:
      - "3000:3000" #specify ports forwarding
    depends_on:
      - database
  database: # name of the third service
    image: mysql:5.7.26 # specify image to build container from
    ports:
      - "127.0.0.1:3306:3306" # specify port forwarding
    environment:
      MYSQL_ROOT_PASSWORD: 'Hbn678%&00^'
